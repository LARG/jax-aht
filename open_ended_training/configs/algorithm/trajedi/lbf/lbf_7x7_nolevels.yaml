defaults:
  - trajedi/_base_
  - _self_ # values from this file override the values from the base file

TOTAL_TIMESTEPS: 1e8 # controls training length; each confederate member sees ~TOTAL_TIMESTEPS / PARTNER_POP_SIZE env steps per iter
LR: 1e-4
CLIP_EPS: 0.1
ENT_COEF: 0.01
TRAJEDI_COEF: 0.0001
