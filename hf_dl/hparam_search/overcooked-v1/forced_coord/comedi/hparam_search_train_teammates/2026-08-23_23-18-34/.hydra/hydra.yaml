hydra:
  run:
    dir: results/${name}/${now:%Y-%m-%d}_${now:%H-%M-%S}
  sweep:
    dir: results_sweep/${name}/${now:%Y-%m-%d}_${now:%H-%M-%S}
    subdir: ${run.seed}
  launcher:
    _target_: hydra._internal.core_plugins.basic_launcher.BasicLauncher
  sweeper:
    _target_: hydra._internal.core_plugins.basic_sweeper.BasicSweeper
    max_batch_size: null
    params: null
  help:
    app_name: ${hydra.job.name}
    header: '${hydra.help.app_name} is powered by Hydra.

      '
    footer: 'Powered by Hydra (https://hydra.cc)

      Use --hydra-help to view Hydra specific help

      '
    template: '${hydra.help.header}

      == Configuration groups ==

      Compose your configuration from those groups (group=option)


      $APP_CONFIG_GROUPS


      == Config ==

      Override anything in the config (foo.bar=value)


      $CONFIG


      ${hydra.help.footer}

      '
  hydra_help:
    template: 'Hydra (${hydra.runtime.version})

      See https://hydra.cc for more info.


      == Flags ==

      $FLAGS_HELP


      == Configuration groups ==

      Compose your configuration from those groups (For example, append hydra/job_logging=disabled
      to command line)


      $HYDRA_CONFIG_GROUPS


      Use ''--cfg hydra'' to Show the Hydra config.

      '
    hydra_help: ???
  hydra_logging:
    version: 1
    formatters:
      simple:
        format: '[%(asctime)s][HYDRA] %(message)s'
    handlers:
      console:
        class: logging.StreamHandler
        formatter: simple
        stream: ext://sys.stdout
    root:
      level: INFO
      handlers:
      - console
    loggers:
      logging_example:
        level: DEBUG
    disable_existing_loggers: false
  job_logging:
    version: 1
    formatters:
      simple:
        format: '[%(asctime)s][%(name)s][%(levelname)s] - %(message)s'
    handlers:
      console:
        class: logging.StreamHandler
        formatter: simple
        stream: ext://sys.stdout
      file:
        class: logging.FileHandler
        formatter: simple
        filename: ${hydra.runtime.output_dir}/${hydra.job.name}.log
    root:
      level: INFO
      handlers:
      - console
      - file
    disable_existing_loggers: false
  env: {}
  mode: RUN
  searchpath: []
  callbacks: {}
  output_subdir: .hydra
  overrides:
    hydra:
    - hydra.mode=RUN
    task:
    - algorithm=comedi/overcooked-v1/forced_coord
    - task=overcooked-v1/forced_coord
    - label=hparam_search_train_teammates
    - algorithm.PARTNER_POP_SIZE=10
    - algorithm.NUM_SEEDS=1
    - algorithm.TRAIN_SEED=30217
    - train_ego=false
    - run_heldout_eval=false
    - logger.mode=online
  job:
    name: run
    chdir: true
    override_dirname: algorithm.NUM_SEEDS=1,algorithm.PARTNER_POP_SIZE=10,algorithm.TRAIN_SEED=30217,algorithm=comedi/overcooked-v1/forced_coord,label=hparam_search_train_teammates,logger.mode=online,run_heldout_eval=false,task=overcooked-v1/forced_coord,train_ego=false
    id: ???
    num: ???
    config_name: base_config_teammate
    env_set: {}
    env_copy: []
    config:
      override_dirname:
        kv_sep: '='
        item_sep: ','
        exclude_keys: []
  runtime:
    version: 1.3.5
    version_base: '1.3'
    cwd: /scratch/08090/clw4542/jax-aht-teammates
    config_sources:
    - path: hydra.conf
      schema: pkg
      provider: hydra
    - path: /scratch/08090/clw4542/jax-aht-teammates/teammate_generation/configs
      schema: file
      provider: main
    - path: ''
      schema: structured
      provider: schema
    output_dir: /scratch/08090/clw4542/jax-aht-teammates/results/overcooked-v1/forced_coord/comedi/hparam_search_train_teammates/2026-08-23_23-18-34
    choices:
      hydra: hydra_simple
      algorithm: comedi/overcooked-v1/forced_coord
      task: overcooked-v1/forced_coord
      hydra/env: default
      hydra/callbacks: null
      hydra/job_logging: default
      hydra/hydra_logging: default
      hydra/hydra_help: default
      hydra/help: default
      hydra/sweeper: basic
      hydra/launcher: basic
      hydra/output: default
  verbose: false
