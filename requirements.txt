# This file is maintained for backward compatibility.
# The canonical dependency specification is in pyproject.toml
# 
# To regenerate this file from pyproject.toml, run:
# pip-compile pyproject.toml
# 
# Or install directly from pyproject.toml:
# pip install -e .

# from jax ai stack v2025.2.5
jax[cuda12]==0.5.3; platform_system == "Linux"
jax==0.5.3; platform_system == "Darwin"
chex==0.1.88
flax==0.10.2
ml_dtypes==0.4.0
optax==0.2.4
orbax-checkpoint==0.11.1
orbax-export==0.0.6

# others
numpy
gymnax
distrax
safetensors
moviepy
prettytable
rliable
matplotlib
# wandb is inherited from upstream jax-aht. The DSSE phase scripts
# (Phase A/B/D/E/F + regenerate_partner_fixtures.sh) all run with
# logger.mode=offline and rely on Sacred + the JSON aggregators in
# writeup/logs/ for results. wandb is only touched by
# common/wandb_visualizations.py, which no DSSE config imports.
wandb
omegaconf
hydra-core
huggingface-hub
setuptools<81

jumanji==1.1.0
jaxmarl==0.0.7
