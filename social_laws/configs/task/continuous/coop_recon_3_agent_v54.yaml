ENV_NAME: continuous/coop_recon_n_agent
ROLLOUT_LENGTH: 128
NUM_EVAL_EPISODES: 50
ENV_KWARGS: {
    instance: "",
    render: true,
    render_name: "coop_recon_n_agent_3_v54",
    render_dir: "render",
    num_agents: 3,
    grid_size: 1.0,
    ego_centric_obs: false,
    movement_noise_std: 0.1,
    done_condition: "all",
    horizon: 100,
    collision_radius: 0.05,
    min_sep_goal: 0.30,
    # NO collision avoidance law — goal-allocation only.
    # social_min_dist defaults to 0.0 = disabled.
    # This is the ablation baseline for V53 (social_min_dist=0.15).
    # All other parameters are identical to V53.
    social_min_dist: 0.0,
  }
TASK_NAME: continuous/coop_recon_n_agent_3_v54
SINGLE_AGENT_1_PROJECTION: ""
SINGLE_AGENT_2_PROJECTION: ""
SINGLE_AGENT_3_PROJECTION: ""
ALPHA_COST: false
