ENV_NAME: continuous/coop_recon_n_agent
ROLLOUT_LENGTH: 128
NUM_EVAL_EPISODES: 200
ENV_KWARGS: {
    instance: "",
    render: true,
    render_name: "coop_recon_n_agent_2_v63",
    render_dir: "render",
    num_agents: 2,
    grid_size: 1.0,
    ego_centric_obs: false,
    movement_noise_std: 0.1,
    done_condition: "all",
    horizon: 100,
    collision_radius: 0.05,
    min_sep_goal: 0.30,
    # Law active at N=2. Expect α≈1.0 (same as V62) — confirms the law
    # adds no overhead or degradation at low density.
    social_min_dist: 0.15,
  }
TASK_NAME: continuous/coop_recon_n_agent_2_v63
SINGLE_AGENT_1_PROJECTION: ""
SINGLE_AGENT_2_PROJECTION: ""
ALPHA_COST: false
