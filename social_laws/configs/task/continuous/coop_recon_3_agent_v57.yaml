ENV_NAME: continuous/coop_recon_n_agent
ROLLOUT_LENGTH: 128
NUM_EVAL_EPISODES: 200
ENV_KWARGS: {
    instance: "",
    render: true,
    render_name: "coop_recon_n_agent_3_v57",
    render_dir: "render",
    num_agents: 3,
    grid_size: 1.0,
    ego_centric_obs: false,
    movement_noise_std: 0.1,
    done_condition: "all",
    horizon: 100,
    collision_radius: 0.05,
    min_sep_goal: 0.30,
    # social_min_dist=0.15: same as V53 but with 200 eval episodes.
    # Purpose: confirm V53's results were not sampling artifacts (α>1.0
    # in some agents was likely due to only 50 eval episodes).
    # Paired baseline: V55 (social_min_dist=0.0, 200 ep).
    social_min_dist: 0.15,
  }
TASK_NAME: continuous/coop_recon_n_agent_3_v57
SINGLE_AGENT_1_PROJECTION: ""
SINGLE_AGENT_2_PROJECTION: ""
SINGLE_AGENT_3_PROJECTION: ""
ALPHA_COST: false
